diff --git a/ios/RNSSearchBar.mm b/ios/RNSSearchBar.mm
index 1111111111111111111111111111111111111111..2222222222222222222222222222222222222222 100644
--- a/ios/RNSSearchBar.mm
+++ b/ios/RNSSearchBar.mm
@@ -46,6 +46,11 @@
 #endif
 
   _controller.searchBar.delegate = self;
+
+#if !TARGET_OS_TV
+  // Keep the native cancel control; hide the in-field clear so search isn't two X's.
+  _controller.searchBar.searchTextField.clearButtonMode = UITextFieldViewModeNever;
+#endif
 
   _isObscureBackgroundSet = NO;
   _isHideNavigationBarSet = NO;
@@ -256,6 +261,7 @@
   if (_textColor != nil) {
     [_controller.searchBar.searchTextField setTextColor:_textColor];
   }
+  _controller.searchBar.searchTextField.clearButtonMode = UITextFieldViewModeNever;
 #endif
 
   [self showCancelButton];
