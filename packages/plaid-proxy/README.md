# @harbor/plaid-proxy

shared plaid proxy wire contract — types + snake→camel mappers used by the worker and the expo app.

```ts
import { mapAccount, mapTransaction, type PlaidAccountDTO } from '@harbor/plaid-proxy';
```
