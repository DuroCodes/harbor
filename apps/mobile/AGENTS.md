# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Package manager: **bun** from the repo root (`bun install`). Do not use npm/npx or commit `package-lock.json`.

Source lives under `src/` (`src/app` for expo-router). Shared plaid types: `@harbor/plaid-proxy`.
