{
  "name": "harbor-plaid-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bash -c 'wrangler dev --config \"$(test -f wrangler.local.toml && echo wrangler.local.toml || echo wrangler.toml)\"'",
    "deploy": "bash -c 'wrangler deploy --config \"$(test -f wrangler.local.toml && echo wrangler.local.toml || echo wrangler.toml)\"'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@harbor/plaid-proxy": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250810.0",
    "typescript": "~5.9.2",
    "wrangler": "^4.120.1"
  }
}
